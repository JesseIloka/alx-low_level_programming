#include "main.h"

/**
 * _strlen - returns the length of a string
 * @s: the variable that holds the string
 *
 * Return: lenght of s
 */
int _strlen(char *s)
{
	int a = 0;
	while (*s++)
		
	{
		a++;
	}
	return (a);
